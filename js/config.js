// Firebase web config — these are PUBLIC client keys (safe to commit; Firestore
// security rules are what actually protect data). Replace with your own project's
// values: Firebase console -> Project settings -> General -> Your apps -> Web app.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
